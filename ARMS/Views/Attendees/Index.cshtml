@model IEnumerable<ARMS.Data.Models.Attendee>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Lecture.LectureID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.User.Username)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BluetoothAddress)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Lecture.LectureID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User.Username)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BluetoothAddress)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AttendeeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AttendeeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AttendeeID })
        </td>
    </tr>
}

</table>
